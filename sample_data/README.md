# Sample Review Data

`sample_reviews.csv` and `sample_reviews.json` are synthetic **SAMPLE DATA** created only for local development, deterministic tests, and offline UI verification.

They are not live App Store reviews and must never be presented as a live, cached, or U.S.-storefront collection result.
